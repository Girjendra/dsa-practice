#include<bits/stdc++.h>
using namespace std;
bool comp(pair<int, int> a, pair<int, int> b){
    // if(a.second < b.second) return true;
    // if(a.second > b.second) return false;
    // if(a.first > b.first) return true;
    // if(a.first < b.first) return false;
}
int main(){
    // vector<int> v = {3,7,4,6,5,1,3,7,8,2,9};

    // sort(v.begin(), v.end()); // Sort full vector in ascending order
    // cout << "Full sorted vector: ";
    // for(int it:v){
    //     cout << it << " ";
    // }

    // sort(v.begin(), v.begin() + 5); // Sort first 5 elements of the vector
    // cout << "\nPartially sorted vector: ";
    // for(int it:v){
    //     cout << it << " ";
    // }

    // sort(v.begin(), v.end(),greater<int>()); // Sort full vector in descending order
    // cout << "\nFull sorted vector in descending order: ";
    // for(int it:v){
    //     cout << it << " ";
    // }
    // sort doesn't work on maps // it is already sorted by default

    // pair<int, int> arr[] = {{1, 2}, {2, 1}, {4, 1}};

    // Sort it according to the second element
    // If second element is same, then sort
    // it according to the first element but in descending order
    
    // sort(arr, arr + 3, comp);
    // cout << "\nSorted array of pairs: ";
    // for(auto it: arr){
    //     cout << "{" << it.first << ", " << it.second << "}, ";
    // }
    // Output: {4, 1}, {2, 1}, {1, 2}   
    
    // int a = 7; // Binary representation: 0111
    // int count = __builtin_popcount(a); // Count number of set bits in a number
    // cout << "\nNumber of set bits in " << a << " is: " << count << endl; // Output: 3

    // long long b = 6542435745374; // Binary representation: 1011101110110010101111101111110111100110
    // long long count1 = __builtin_popcountll(b); // Count number of set bits in a long long number
    // cout << "Number of set bits in " << b << " is: " << count1 << endl; // Output: 3

    // string s = "123";
    // do{
    //     cout<<s<<" ";
    // }while(next_permutation(s.begin(), s.end()));  // gives all next permutations of the string not previous one
    //                                                // string sould be sorted in ascending order to get all permutations
    
    // vector<int> v = {3,7,4,6,5,0,3,7,8,2,9};
    // int max = *max_element(v.begin(),v.end()); // Find maximum element in the vector
    // cout << "\nMaximum element in the vector: " << max << endl; // Output: 9

    // int min = *min_element(v.begin(),v.end()); // Find minimum element in the vector
    // cout << "Minimum element in the vector: " << min << endl; // Output: 0

    return 0;   
}