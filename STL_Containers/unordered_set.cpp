#include<bits/stdc++.h>
using namespace std;
int main(){
    unordered_set<int> st;

    /*
    lower_bound and upper_bound function does not work, 
    rest all functions are same as above. It does not 
    store in any particular order. It has a better complexity
    than set in most cases, except some when collision happens.
    */
    return 0; 
}